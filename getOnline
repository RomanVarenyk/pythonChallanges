statuses = {
    "Alice": "online",
    "Bob": "offline",
    "Eve": "online",
}
def online_count(statuss):
    count = 0
    thing = list(statuss.keys())
    num = 0
    for i in statuss:
        returned = (statuss.get(thing.__getitem__(num)))
        num = num +1
        if returned == 'online':
            count = count +1
    return count
print(online_count(statuses))
